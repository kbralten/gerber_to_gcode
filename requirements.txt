# Core dependencies for Gerber outline parsing
pygerber>=2.0.0
shapely>=2.0.0

# Python 3.6 or higher is required for:
# - f-strings
# - Type hints (typing module)
# - argparse
# - math, re, sys modules
